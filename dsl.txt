a=10
if(a==10)
{
	job("myfirstjob"){
		description("this is my discription")
		
		scm{
			github('AkarshAgarwal99/byfilesystem','master')
			}
		triggers{
			scm("* * * * *")
			}
		steps{
          shell('''if sudo docker ps -a | grep firstos
                then
				sudo docker rm -f firstos
				sudo docker run -dit --name firstos centos:latest
				else 
				sudo docker run -dit --name firstos centos:latest
				fi''')
			}
		}
}